# Agent source template — placeholder for v1
# Full YAML templating defined in separate REQ (IDEA-004)
name: spec-writer
description: "Converts features into atomic, testable requirements."
model: sonnet
